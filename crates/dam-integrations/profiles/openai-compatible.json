{
  "id": "openai-compatible",
  "name": "Generic OpenAI-compatible harness",
  "summary": "Route normal OpenAI-compatible HTTPS traffic through DAM.",
  "provider": "openai-compatible",
  "traffic_app_ids": ["openai-api"],
  "connect_args": [
    "--openai",
    "--network-mode",
    "tun",
    "--trust-mode",
    "local_ca"
  ],
  "settings": [
    {
      "key": "HTTPS_PROXY",
      "value": "{{proxy_url}}",
      "description": "HTTPS proxy for explicit-proxy fallback"
    },
    {
      "key": "HTTP_PROXY",
      "value": "{{proxy_url}}",
      "description": "HTTP proxy for explicit-proxy fallback"
    }
  ],
  "commands": [
    {
      "label": "Start DAM for OpenAI-compatible traffic",
      "command": [
        "dam",
        "connect",
        "--openai",
        "--network-mode",
        "tun",
        "--trust-mode",
        "local_ca"
      ]
    }
  ],
  "notes": [
    "Keep provider credentials owned by the harness. DAM forwards caller auth headers.",
    "Tray/web Connect installs Network Extension capture first; HTTPS_PROXY / HTTP_PROXY remains the explicit-proxy fallback for source builds and unsupported environments."
  ],
  "automation": "connect_preset"
}
