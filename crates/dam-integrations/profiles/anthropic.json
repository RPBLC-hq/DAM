{
  "id": "anthropic",
  "name": "Generic Anthropic-compatible harness",
  "summary": "Route normal Anthropic HTTPS traffic through DAM.",
  "provider": "anthropic",
  "traffic_app_ids": ["anthropic-api"],
  "connect_args": [
    "--anthropic",
    "--network-mode",
    "tun",
    "--trust-mode",
    "local_ca"
  ],
  "settings": [
    {
      "key": "HTTPS_PROXY",
      "value": "{{proxy_url}}",
      "description": "HTTPS proxy for explicit-proxy fallback"
    },
    {
      "key": "HTTP_PROXY",
      "value": "{{proxy_url}}",
      "description": "HTTP proxy for explicit-proxy fallback"
    }
  ],
  "commands": [
    {
      "label": "Start DAM for Anthropic traffic",
      "command": [
        "dam",
        "connect",
        "--anthropic",
        "--network-mode",
        "tun",
        "--trust-mode",
        "local_ca"
      ]
    }
  ],
  "notes": [
    "Keep provider credentials owned by the harness. DAM forwards caller auth headers.",
    "Tray/web Connect installs Network Extension capture first; HTTPS_PROXY / HTTP_PROXY remains the explicit-proxy fallback for source builds and unsupported environments."
  ],
  "automation": "connect_preset"
}
